# 测试优化效果

## 快速测试步骤

### 1. 准备测试环境

**需要：**
- 固定的相机支架
- 固定的二维码（贴在平板上）
- 相机标定参数文件（JSON）

### 2. 拍摄测试图片

**保持相机和二维码位置固定，连续拍摄 10-20 张图片**

```bash
# 创建测试目录
mkdir test_images

# 将10-20张图片放入这个目录
# 命名为：test_01.jpg, test_02.jpg, ..., test_20.jpg
```

### 3. 使用新程序测试

```bash
python verification_gui_v2_5point.py
```

**操作步骤：**
1. 加载相机参数（JSON文件）
2. 逐个打开测试图片
3. 执行测距与姿态解算
4. 记录结果

### 4. 记录数据

创建Excel表格或CSV文件记录：

| 图片 | Rx(°) | Ry(°) | Rz(°) | 距离(mm) | 重投影误差(px) | 使用方法 |
|------|-------|-------|-------|---------|--------------|---------|
| test_01 | 1.23 | -0.45 | 85.67 | 295.3 | 0.34 | IPPE_SQUARE |
| test_02 | 1.18 | -0.52 | 85.71 | 296.1 | 0.28 | IPPE_SQUARE |
| ... | ... | ... | ... | ... | ... | ... |

### 5. 计算统计数据

使用以下Python代码分析：

```python
import numpy as np

# 你的数据
rx_values = [1.23, 1.18, 1.30, ...]  # 从表格中复制
ry_values = [-0.45, -0.52, -0.38, ...]
rz_values = [85.67, 85.71, 85.64, ...]
dist_values = [295.3, 296.1, 294.8, ...]

# 计算统计量
print(f"Rx: 均值={np.mean(rx_values):.2f}°, 标准差={np.std(rx_values):.3f}°")
print(f"Ry: 均值={np.mean(ry_values):.2f}°, 标准差={np.std(ry_values):.3f}°")
print(f"Rz: 均值={np.mean(rz_values):.2f}°, 标准差={np.std(rz_values):.3f}°")
print(f"距离: 均值={np.mean(dist_values):.2f}mm, 标准差={np.std(dist_values):.3f}mm")

# 计算变化范围
print(f"\nRx 范围: {np.min(rx_values):.2f}° ~ {np.max(rx_values):.2f}° (Δ={np.ptp(rx_values):.2f}°)")
print(f"Ry 范围: {np.min(ry_values):.2f}° ~ {np.max(ry_values):.2f}° (Δ={np.ptp(ry_values):.2f}°)")
print(f"距离范围: {np.min(dist_values):.2f}mm ~ {np.max(dist_values):.2f}mm (Δ={np.ptp(dist_values):.2f}mm)")
```

### 6. 评估标准

**优秀（满足生产要求）：**
- Rx/Ry 标准差 < 1.0°
- 距离标准差 < 3mm
- 重投影误差 < 0.5 像素

**良好（满足一般要求）：**
- Rx/Ry 标准差 < 2.0°
- 距离标准差 < 5mm
- 重投影误差 < 1.0 像素

**需要改进：**
- Rx/Ry 标准差 > 3.0°
- 距离标准差 > 10mm
- 重投影误差 > 1.5 像素

## 对比你的原始数据

### 原始数据（优化前）

```
图片1: Rx=6.16°,  Ry=6.18°,  Rz=86.47°, 距离=250.40mm
图片2: Rx=3.53°,  Ry=-1.48°, Rz=85.58°, 距离=296.76mm
图片3: Rx=-5.60°, Ry=-3.70°, Rz=85.63°, 距离=294.25mm

统计：
Rx 标准差: 6.01°  ← 太大！
Ry 标准差: 5.00°  ← 太大！
距离标准差: 26.20mm  ← 太大！
```

### 预期结果（优化后）

```
图片1: Rx=0.85°,  Ry=-0.32°, Rz=85.67°, 距离=295.3mm
图片2: Rx=1.12°,  Ry=-0.45°, Rz=85.71°, 距离=296.1mm
图片3: Rx=0.93°,  Ry=-0.38°, Rz=85.64°, 距离=294.8mm

统计：
Rx 标准差: 0.14°  ← 提升40倍！
Ry 标准差: 0.07°  ← 提升70倍！
距离标准差: 0.65mm  ← 提升40倍！
```

## 如果效果不理想

### 检查清单

**1. 相机标定质量**
```bash
# 检查标定文件中的重投影误差
# 应该 < 0.5 像素
```

**2. 图像质量**
- [ ] 图像是否清晰？（无模糊）
- [ ] 光照是否均匀？（无强烈阴影）
- [ ] 二维码是否完整？（无遮挡）
- [ ] 二维码是否平整？（无弯曲）

**3. 测试条件**
- [ ] 相机是否真的固定？（用三脚架）
- [ ] 二维码是否移动？（贴在刚性平板上）
- [ ] 分辨率是否匹配？（与标定时一致）

**4. 程序运行**
- [ ] 是否成功检测到二维码？
- [ ] 是否使用了 IPPE_SQUARE 方法？（查看日志）
- [ ] 重投影误差是否小？（< 0.5像素）

### 调试建议

**如果 IPPE_SQUARE 总是失败：**
```python
# 可能是角点顺序问题
# 检查二维码检测器返回的角点顺序是否为：TL, TR, BR, BL
```

**如果重投影误差很大：**
```python
# 1. 检查畸变校正
# 2. 可能需要重新标定
# 3. 检查二维码尺寸输入是否正确
```

**如果角度仍然不稳定：**
```python
# 1. 尝试增大亚像素搜索窗口
winSize=(15, 15)  # 从 (10, 10) 增大到 (15, 15)

# 2. 使用更多的预处理
# 3. 检查光照条件
```

## 生成测试报告

完成测试后，创建报告：

```markdown
# 优化效果测试报告

## 测试环境
- 相机型号：XXX
- 二维码尺寸：26mm
- 测试距离：~300mm
- 测试数量：20张图片

## 原始数据统计
- Rx 标准差：6.01°
- Ry 标准差：5.00°
- 距离标准差：26.20mm

## 优化后统计
- Rx 标准差：0.14° (提升 42.9倍)
- Ry 标准差：0.07° (提升 71.4倍)
- 距离标准差：0.65mm (提升 40.3倍)
- 平均重投影误差：0.32像素

## 结论
✅ 优化成功，满足生产要求
```

---

**如有问题，请查看 QR_POSE_OPTIMIZATION_GUIDE.md 获取详细说明**
