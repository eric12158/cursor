# 🤖 机器人视觉引导系统使用说明

## 🎯 核心功能

**将二维码/AprilTag的位姿转换到机械臂基座坐标系，实现视觉引导抓取！**

---

## 📊 数据流程图

```
相机拍照 
   ↓
检测二维码/AprilTag → 得到：二维码在相机系的位姿
   ↓
手眼标定矩阵 (T_Camera_to_Flange) → 相机与法兰的固定关系
   ↓
机械臂实时位姿 (T_Flange_to_Base) → 从机械臂控制器读取
   ↓
坐标变换计算: Base ← Flange ← Camera ← QR
   ↓
得到：二维码在基座系的位姿 ✅
   ↓
生成抓取位姿（在目标上方50mm）
   ↓
发送给机械臂执行 🎉
```

---

## 🚀 三种使用方式

### 方式1：独立计算工具（推荐学习）

```bash
python hand_eye_transform.py
```

**优点：**
- 清晰展示每一步计算
- 便于理解坐标变换原理
- 可以修改代码适配自己的需求

**适合：** 学习理解、调试验证

---

### 方式2：集成GUI工具（推荐实用）

```bash
python robot_vision_integrated.py
```

**功能：**
1. ✅ 加载相机参数
2. ✅ 加载手眼标定矩阵
3. ✅ 检测二维码/AprilTag
4. ✅ 输入机械臂实时位姿
5. ✅ 计算目标在基座系位姿
6. ✅ 生成推荐抓取位姿

**优点：**
- 一站式完成所有操作
- 可视化显示检测结果
- 实时计算坐标变换

**适合：** 实际应用、快速测试

---

### 方式3：原始检测工具

```bash
# QR码
python verification_gui_v2_ultra.py

# AprilTag
python verification_gui_apriltag.py
```

**功能：** 只检测二维码/AprilTag，得到相机系位姿

**适合：** 单独测试检测精度

---

## 📝 完整使用流程（以集成GUI为例）

### 步骤1️⃣：准备数据

**需要准备3个文件：**

1. **相机标定文件** (`camera_params.json`)
   ```json
   {
     "camera_matrix": [...],
     "dist_coeffs": [...],
     "image_size": [width, height]
   }
   ```

2. **手眼标定文件** (`handeye_calibration.json`)
   ```json
   {
     "translation": {"x": -21.0093, "y": -7.6736, "z": 576.7124},
     "rotation": {"rx": 20.8621, "ry": 11.3074, "rz": 117.7986}
   }
   ```
   *示例已提供：`handeye_calibration_example.json`*

3. **测试图片**（含有二维码/AprilTag）

---

### 步骤2️⃣：运行程序

```bash
python robot_vision_integrated.py
```

---

### 步骤3️⃣：操作流程

#### 1. 加载相机参数
- 点击 **"加载相机标定文件"**
- 选择你的相机参数JSON
- 看到 ✅ "已加载" 提示

#### 2. 加载手眼标定
- 点击 **"加载手眼标定结果"**
- 选择 `handeye_calibration_example.json`
- 或手动输入你的标定数据
- 看到 ✅ "已加载" 提示

#### 3. 设置检测参数
- **标签家族**: 选择 "自动检测"（或具体家族）
- **标签边长**: 输入实际尺寸（如 26.0mm）

#### 4. 打开图片
- 点击 **"📷 打开图片"**
- 选择含有标签的图片

#### 5. 执行检测
- 点击 **"🎯 执行检测"**
- 查看右侧结果：
  ```
  ✅ 检测到 1 个AprilTag
  📍 标签在相机坐标系的位姿:
     平移: X=-15.35, Y=17.04, Z=304.30 mm
     旋转: Rx=5.24°, Ry=1.95°, Rz=-3.24°
  ```

#### 6. 输入机械臂位姿
⚠️ **关键步骤！** 需要从机械臂控制器读取**当前实时位姿**

**示例（替换为实际值）：**
- X(mm): 300.0
- Y(mm): 100.0
- Z(mm): 400.0
- Rx(°): 180.0
- Ry(°): 0.0
- Rz(°): 90.0

#### 7. 计算目标位姿
- 点击 **"🤖 计算目标在基座系位姿"**
- 查看结果：
  ```
  🎯 目标在基座坐标系的位姿:
     平移: X=XXX.XX, Y=XXX.XX, Z=XXX.XX mm
     旋转: Rx=XX.XX°, Ry=XX.XX°, Rz=XX.XX°
  
  💡 可以将此位姿发送给机械臂执行移动！
  ```

#### 8. 生成抓取位姿（可选）
- **接近高度偏移**: 输入 50（在目标上方50mm）
- 点击 **"✨ 生成抓取位姿"**
- 得到推荐的抓取位姿

---

## 💡 关键概念解释

### 1. 坐标系关系

```
机械臂基座坐标系 (Base)
   ↓ T_Flange_to_Base (实时变化，从机械臂读取)
法兰坐标系 (Flange)
   ↓ T_Camera_to_Flange (固定，手眼标定得到)
相机坐标系 (Camera)
   ↓ T_QR_to_Camera (实时检测得到)
二维码坐标系 (QR)
```

### 2. 手眼标定矩阵

**作用：** 描述相机相对于法兰的固定安装关系

**你的数据：**
```
平移: X=-21.0093, Y=-7.6736, Z=576.7124 mm
旋转: Rx=20.8621°, Ry=11.3074°, Rz=117.7986°
```

**含义：**
- 相机安装在法兰上
- 相机光心距离法兰中心 576mm（Z方向）
- 有一定的旋转偏移

### 3. 坐标变换公式

```python
T_QR_to_Base = T_Flange_to_Base @ T_Camera_to_Flange @ T_QR_to_Camera
```

**矩阵乘法从右往左读：**
1. 二维码 → 相机
2. 相机 → 法兰  
3. 法兰 → 基座

---

## 🎯 实际应用场景

### 场景1：视觉引导抓取

**流程：**
1. 相机拍照识别目标物体（贴有AprilTag）
2. 计算物体在基座系的位姿
3. 规划抓取路径
4. 机械臂移动到目标位置
5. 执行抓取

**代码：**
```python
# 得到目标在基座系位姿
target_pose = (tx, ty, tz, rx, ry, rz)

# 生成抓取位姿（上方50mm）
grasp_pose = (tx, ty, tz+50, rx, ry, rz)

# 发送给机械臂
robot.move_to(grasp_pose)  # 接近
robot.move_to((tx, ty, tz, rx, ry, rz))  # 下降
robot.gripper.close()  # 抓取
robot.move_to(grasp_pose)  # 提升
```

### 场景2：自动化装配

**应用：** 精确定位零件位置，实现自动装配

### 场景3：物料分拣

**应用：** 识别并抓取传送带上的物体

### 场景4：质量检测

**应用：** 测量物体位姿偏差，判断是否合格

---

## ⚠️ 常见问题

### Q1: 计算出的位姿不准确？

**可能原因：**
1. ❌ 手眼标定质量差 → 重新标定
2. ❌ 二维码检测不准 → 使用超稳定版工具
3. ❌ 机械臂位姿读取错误 → 检查通信
4. ❌ 坐标系定义不一致 → 统一坐标系定义

### Q2: 机械臂实时位姿从哪里读取？

**答：** 需要从机械臂控制器实时读取

**示例代码（伪代码）：**
```python
# 不同品牌机械臂接口不同
current_pose = robot.get_current_pose()  # 返回 (x,y,z,rx,ry,rz)
```

**常见品牌：**
- UR机器人：`robot.getActualTCPPose()`
- KUKA：读取`$POS_ACT`
- ABB：读取`CRobT`
- 遨博：`get_current_pose()`

### Q3: 手眼标定矩阵是怎么来的？

**答：** 通过手眼标定程序计算得到

**流程：**
1. 在不同位置拍摄标定板
2. 记录机械臂位姿
3. 运行标定算法（AX=XB）
4. 得到 T_Camera_to_Flange

**参考工具：**
- `hand_eye_tool.py`（如果你的项目中有）
- MoveIt 手眼标定
- Easy-Hand-Eye-Calibration

### Q4: 为什么需要手眼标定？

**答：** 建立相机与机械臂的空间关系

**没有手眼标定的后果：**
- ❌ 相机看到的物体位置
- ❌ 无法转换到机械臂坐标系
- ❌ 机械臂不知道该往哪移动

**有了手眼标定：**
- ✅ 相机 ← 手眼标定 → 机械臂
- ✅ 可以自由转换坐标系
- ✅ 实现视觉引导

---

## 📚 文件说明

| 文件 | 用途 |
|------|------|
| `robot_vision_integrated.py` | ⭐ 集成GUI工具（推荐） |
| `hand_eye_transform.py` | 独立计算工具（学习用） |
| `verification_gui_v2_ultra.py` | QR码检测（超稳定版） |
| `verification_gui_apriltag.py` | AprilTag检测 |
| `handeye_calibration_example.json` | 手眼标定示例文件 |

---

## 🎓 进阶学习

### 理解齐次变换矩阵

4×4变换矩阵结构：
```
[R11  R12  R13  | Tx]
[R21  R22  R23  | Ty]
[R31  R32  R33  | Tz]
[  0    0    0  |  1]
```
- 左上3×3：旋转矩阵 (R)
- 右侧1×3：平移向量 (T)

### 矩阵乘法顺序

**重要：** 坐标变换矩阵乘法**从右往左**计算

```python
# 错误 ❌
T_result = T_A @ T_B @ T_C  # 先A再B再C

# 正确 ✅
T_result = T_C @ T_B @ T_A  # 先A再B再C（从右往左读）
```

---

## 🏆 总结

### 核心价值

**手眼标定 + 二维码检测 = 视觉引导抓取**

### 数据流

1. 相机检测 → 二维码在相机系位姿
2. 手眼标定 → 相机到法兰的变换
3. 机械臂位姿 → 法兰到基座的变换
4. **坐标变换 → 二维码在基座系位姿** ✅
5. 发送给机械臂 → 执行抓取 🎉

### 立即开始

```bash
# 集成工具（推荐）
python robot_vision_integrated.py

# 独立计算（学习）
python hand_eye_transform.py
```

**祝你成功实现视觉引导！🚀**
